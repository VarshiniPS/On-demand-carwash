#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mongodb-prebuilt/built/bin/bsondump.js" "$@"
  ret=$?
else 
  node  "$basedir/../mongodb-prebuilt/built/bin/bsondump.js" "$@"
  ret=$?
fi
exit $ret
